{% extends 'base.html.twig' %}

{% block header_title %}Inscription{% endblock %}

{% block body %}
    <h1>Inscription</h1>

    {{ form_start(form, {'action' : path('register')}) }}
        {{ form_widget(form) }}

        <button type="submit" class="btn btn-success pull-right">Ajouter</button>
    {{ form_end(form) }}
{% endblock %}
